Useful commands on this Darwin workstation: `pwd`, `ls`, `cd`, `rg`, `find`, `git status`, `git diff`. Backend: `make build`, `make build-prod`, `make run`, `make run-migrate`, `make test`, `make test-db`, `go test ./...`, `go test ./internal/handlers -run '<pattern>'`. Frontend: `cd frontend && npm install`, `cd frontend && npm run dev`, `cd frontend && npm run build`, `cd frontend && npm run typecheck`, `cd frontend && npx vitest run <tests>`, `cd frontend && npm run test:e2e`. Full dev loop: `make frontend-dev`, `make backend-watch`, or `make run-migrate` for local server with migrations. Deployment builds typically run `VERSION=<tag> GOTOOLCHAIN=go1.25.8+auto make build-prod`.