[hub_database_init] OVER-ENGINEERING — `cmd/whatomate/main.go` is a composition root that wires dependencies; DB, Redis, queue, and WebSocket hub are all initialized sequentially in `runServer` (e.g., DB at lines 148-153, hub at 190-193). That’s normal app bootstrap wiring, not a cross-module “hub initialized inside database” coupling.  
[monster_handler_files] GENUINE — Multiple handler files are extremely large and mix request parsing, DB logic, and response shaping. For example: `internal/handlers/chatbot_processor.go` is 2912 lines and `internal/handlers/agent_transfers.go` is 1465 lines (per `wc -l`). `agent_transfers.go` alone includes request/response structs, SQL joins, permission checks, pagination, and websocket usage in a single file (see imports/structs around lines 1-120).  
[pkg_boundary_leak] GENUINE — `pkg/whatsapp/adapter.go` imports `internal/models` and `gorm` (`pkg/whatsapp/adapter.go` lines 7-10), and `pkg/whatsmeow/manager.go` imports `internal/config`, `internal/models`, `internal/queue`, and `internal/websocket` (`pkg/whatsmeow/manager.go` lines 10-13). That’s a clear internal-package dependency leak from `pkg/` outward.  
[stringly_typed_statuses] EXAGGERATED — There is strong typed status usage (e.g., `MessageStatus`, `InstanceStatus`, `TransferStatus` in `internal/models/constants.go` lines 38-155), but several status fields remain plain `string` with comment-only enums (e.g., `WhatsAppAccount.Status` line 300, `Template.Status` line 406, `WhatsAppFlow.Status` line 429 in `internal/models/models.go`). So the issue exists, but it’s not “all statuses are stringly-typed.”  
[untyped_jsonb_settings] GENUINE — `internal/models/models.go` defines `type JSONB map[string]interface{}` (lines 13-15) and uses `Settings JSONB` on `Organization` and `User` (lines 92 and 112). These settings are untyped, schema-less maps.