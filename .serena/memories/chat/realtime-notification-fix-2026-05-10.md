Realtime chat notification fix: incoming `new_message` handling now shows toast notifications even when the active chat is open, and NotificationBell unread chat list now aggregates loaded contacts across `contacts`, `pendingChats`, `assignedChats`, and `closedChats` instead of only `sortedContacts` from the active sidebar tab. Backend `broadcastNewMessage` and inbound `saveIncomingMessage` payloads include `whatsapp_account`; frontend `handleNewMessage` preserves it before store insertion so account-filtered chat threads stay aligned with realtime events. Tests added in `frontend/src/services/websocket.test.ts` and `frontend/src/components/NotificationBell.test.ts`. Verification: focused Vitest passed, frontend build passed, chat Playwright spec passed serially, app Go packages passed excluding pre-existing `tmp` scratch package failure.