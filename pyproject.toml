[project]
name = "whatomate"
version = "0.1.0"
description = "A CocoIndex application"
requires-python = ">=3.11"
dependencies = [
    "cocoindex>=1.0.3",
    "sqlite-vec",
    "sentence-transformers",
]
