---
title: المراقبة
rtl: true
lang: ar
---

<div dir="rtl">المراقبة</div>

يوفر Whatomate فحوصات صحة مدمجة وتسجيل ومقاييس للمراقبة التشغيلية.

## نقاط نهاية الصحة

### فحص الصحة

```
GET /health
```

يعيد حالة توفر الخدمة الأساسية:

```json
{
  "status": "ok",
  "service": "whatomate"
}
```

لا تتحقق نقطة النهاية هذه من التوابع — تؤكد فقط أن خادم HTTP يعمل.

### فحص الجاهزية

```
GET /ready
```

يتحقق من توفر جميع التوابع:

```json
{
  "status": "ready"
}
```

أو عند الفشل (HTTP 500):

```json
{
  "status": "not ready",
  "error": "database connection failed"
}
```

يجري فحص الجاهزية:
1. **ping قاعدة البيانات** — التحقق من اتصال PostgreSQL
2. **ping لـ Redis** — التحقق من اتصال Redis

## تسجيل الطلبات

يتم تسجيل كل طلب HTTP مع:

| الحقل | الوصف |
|-------|-------------|
| Method | طريقة HTTP (GET، POST، إلخ) |
| Path | مسار الطلب |
| Remote Address | عنوان IP للعميل |
| Status Code | حالة الاستجابة |
| Duration | وقت معالجة الطلب |
| Request ID | معرف التتبع الفريد |
| User ID | المستخدم المصادق عليه (إن وجد) |

مثال على مخرج السجل:

```
2024-01-01T12:00:00Z INFO request method=POST path=/api/contacts/123/messages status=201 duration=45ms request_id=abc123 user_id=5
```

## سجلات النشاط

يتم تسجيل الإجراءات المهمة في جدول `activity_logs`:

| الحقل | الوصف |
|-------|-------------|
| user_id | من نفذ الإجراء |
| action | نوع الإجراء (create, update, delete, login, إلخ) |
| resource | نوع المورد (user, contact, campaign, إلخ) |
| resource_id | معرف المورد المتأثر |
| details | سياق إضافي (JSON) |
| created_at | الطابع الزمني |

عرض سجلات النشاط عبر API:

```bash
curl https://whatomate.example.com/api/activity-logs \
  -H "Authorization: Bearer <token>"
```

يتم الاحتفاظ بسجلات النشاط لمدة 90 يوماً بشكل افتراضي، مع تنظيف تلقائي بواسطة عامل الاحتفاظ.

## سجلات API

تتضمن سجلات طلب/استجابة API:

- جسم الطلب (للتصحيح)
- جسم الاستجابة (للتحقيق في الأخطاء)
- حالة المصادقة
- نتائج فحص الصلاحيات

تُكتب السجلات إلى stdout/stderr ويمكن التقاطها بواسطة بنية التسجيل الخاصة بك.

## مراقبة اتصال WebSocket

يتتبع WebSocket Hub الاتصالات النشطة:

| المقياس | الوصف |
|--------|-------------|
| إجمالي الاتصالات | اتصالات WebSocket النشطة |
| اتصالات لكل مؤسسة | اتصالات مجمعة حسب المؤسسة |
| مدة الاتصال | مدة نشاط كل اتصال |
| أسباب الانقطاع | سبب إغلاق الاتصالات |

يتم تسجيل أحداث الاتصال:

```
2024-01-01T12:00:00Z INFO websocket connected user_id=5 org_id=1
2024-01-01T12:05:00Z INFO websocket disconnected user_id=5 reason="client closed"
```

## مقاييس صحة النسخ

يتم تتبع صحة نسخ WhatsApp ومتاح عبر API:

```bash
curl https://whatomate.example.com/api/instances/{id}/health \
  -H "Authorization: Bearer <token>"
```

الاستجابة:

```json
{
  "instance_id": 1,
  "status": "connected",
  "uptime": "24h15m",
  "messages_sent_today": 1250,
  "messages_received_today": 890,
  "messages_failed_today": 3,
  "error_rate": "0.24%",
  "queue_depth": 12
}
```

| المقياس | الوصف |
|--------|-------------|
| `status` | حالة الاتصال (disconnected, connecting, connected, qr, paired) |
| `uptime` | الوقت منذ آخر اتصال ناجح |
| `messages_sent_today` | الرسائل الصادرة المرسلة |
| `messages_received_today` | الرسائل الواردة المستلمة |
| `messages_failed_today` | محاولات الإرسال الفاشلة |
| `error_rate` | نسبة الرسائل الفاشلة |
| `queue_depth` | عمق طابور الرسائل الحالي |

## إحصائيات الحملات

يتم تتبع تقدم الحملة في الوقت الفعلي:

```bash
curl https://whatomate.example.com/api/campaigns/{id} \
  -H "Authorization: Bearer <token>"
```

تتضمن الاستجابة:

```json
{
  "id": 1,
  "name": "Welcome Campaign",
  "status": "running",
  "total_recipients": 500,
  "sent_count": 245,
  "failed_count": 3,
  "delivered_count": 240,
  "read_count": 180,
  "progress_percentage": 49,
  "started_at": "2024-01-01T10:00:00Z"
}
```

يتم بث إحصائيات الحملة عبر WebSocket في الوقت الفعلي أثناء معالجة العمال للمستلمين.

## مراقبة العمليات في الخلفية

تعمل العمليات في الخلفية التالية بشكل مستمر:

| العملية | الفاصل | الغرض | المراقبة |
|---------|----------|---------|------------|
| معالج SLA | دقيقة واحدة | فحص خروقات SLA، إغلاق المحادثات تلقائياً | إدخالات سجل عند الخرق |
| احتفاظ النشاط | ساعة واحدة | حذف سجلات النشاط القديمة | إدخالات سجل عند التنظيف |
| إعادة تعيين تعيين المحادثة | دقيقة واحدة | إعادة تعيين التعيينات القديمة | إدخالات سجل عند إعادة التعيين |
| حملة النسخ التلقائية | دقيقة واحدة | إرسال رسائل آلية | إدخالات سجل عند الإرسال |
| عامل الحملة | مستمر | معالجة طابور الحملة | عمق الطابور، الإنتاجية |
| عامل الوسائط الواردة | مستمر | تنزيل الوسائط الواردة | عمق الطابور |
| مشترك إحصائيات الحملة | مستمر | بث إحصائيات الحملة عبر WS | حالة الاتصال |
| إعادة اتصال WhatsMeow | عند البدء | إعادة اتصال جميع النسخ | حالة النسخ |
| التوفيق بين الحالات | عند البدء (مهلة 30 ثانية) | تنظيف حالات النسخ القديمة | إدخالات سجل |

راقب العمليات في الخلفية عبر السجلات:

```bash
# تصفية خروقات SLA
docker compose logs whatomate | grep "SLA"

# تصفية معالجة الحملة
docker compose logs whatomate | grep "campaign"

# تصفية اتصالات النسخ
docker compose logs whatomate | grep "instance"
```

## تكاملات المراقبة

### مقاييس Prometheus (مستقبلاً)

تصدير المقاييس بتنسيق Prometheus للمراقبة الخارجية:

- عدد الطلبات حسب نقطة النهاية
- مدرجات مدة الطلب
- اتصالات WebSocket النشطة
- عمق طابور الحملة
- حالة اتصال النسخ
- معدلات الخطأ

### تجميع السجلات

أعد توجيه السجلات إلى نظام تجميع السجلات الخاص بك:

```bash
# مشغل تسجيل Docker
docker compose up --log-driver fluentd

# أو التوجيه إلى خدمة خارجية
docker compose logs -f whatomate | fluent-cat whatomate.logs
```

## توصيات التنبيه

قم بإعداد تنبيهات لـ:

| الحالة | الخطورة | الإجراء |
|-----------|----------|--------|
| `/ready` يعيد 500 | حرج | التحقق من اتصال قاعدة البيانات/Redis |
| النسخ منفصل > 5 دقائق | عالي | التحقق من اتصال WhatsApp |
| معدل خطأ الحملة > 5% | عالي | التحقق من حالة القالب/الحساب |
| عمق الطابور > 1000 | تحذير | توسيع العمال أو التحقق من التراكم |
| مساحة القرص < 10% | تحذير | تنظيف تخزين الوسائط |
| استخدام الذاكرة > 90% | تحذير | التحقق من تسرب الذاكرة |

## انظر أيضاً

- [فحوصات الصحة والجاهزية](deployment.md#health-and-readiness-endpoints) — تفاصيل نقاط النهاية
- [استكشاف الأخطاء](troubleshooting.md) — تشخيص المشاكل الشائعة
- [النشر](deployment.md) — إعداد الإنتاج مع المراقبة
