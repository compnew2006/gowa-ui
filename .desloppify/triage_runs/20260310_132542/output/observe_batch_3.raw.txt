[review::convention::boilerplate_utility_duplication] GENUINE — `internal/handlers/message_template_placeholders.go` and `internal/worker/campaign_template_placeholders.go` both implement near‑identical placeholder detection (`detect...Usage`, `contains...Placeholder`), replacement (`renderMessageTemplate` vs `renderCampaignTemplateBody`), and resolver helpers (`resolve...CustomerName`, `resolve...ContactName`, `resolve...PhoneNumber`, `resolve...ChatID`, plus org/agent name resolution), differing mostly in naming and defaults.

[review::convention::settings_resolution_duplication] EXAGGERATED — instance settings parsing in `pkg/whatsmeow/instance_settings.go`, `pkg/whatsmeow/instance_auto_reject_settings.go`, and `pkg/whatsmeow/instance_auto_campaign_settings.go` shares common helpers (`mapFromAny`, `boolFromAny`, `stringFromAny`, `intFromAny`) defined once in `instance_auto_reject_settings.go`; auto‑campaign uses those helpers rather than duplicating them. I did not find copy‑pasted “settings resolution” blocks for the same domain.

[review::mid_level::monolithic_orchestration] GENUINE — `cmd/whatomate/main.go` `runServer` and `runWorker` are long orchestration functions (~400+ lines each) that inline config load/validate, DB/Redis setup, queue, websocket, whatsmeow initialization, routing/middleware, workers, and shutdown logic in one place.

[review::mid_level::rigid_app_seams] GENUINE — `internal/handlers/app.go`’s `App` struct is wired to concrete types (`*gorm.DB`, `*redis.Client`, `*whatsapp.Client`, `*whatsmeow.ConnectionManager`, `*sqlstore.Container`, `queue.Queue`, etc.) and `cmd/whatomate/main.go` constructs concrete implementations directly; handlers therefore have tight coupling and limited seam flexibility for alternate implementations or tests.