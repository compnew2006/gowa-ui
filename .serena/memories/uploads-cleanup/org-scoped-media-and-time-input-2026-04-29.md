Uploads cleanup bug fixed on 2026-04-29: local outgoing media is saved under `orgs/<org-id>/<media-type>/...` by `organizationMediaSubdir`, but cleanup only scanned legacy root-level `audio/documents/images/stickers/videos`. The worker now also scans `uploads/orgs/<org-id>/{audio,documents,images,stickers,videos}` while preserving symlink skipping and excluding non-target dirs such as `chat-backgrounds`. The Uploads Cleanup UI now renders Daily Cleanup Hour as a `type="time"` whole-hour input (`HH:MM`) but still sends/stores backend `uploads_cleanup_schedule_hour` as an integer for compatibility. Regression coverage exists in `internal/handlers/uploads_cleanup_worker_test.go`, `frontend/src/views/settings/SettingsView.test.ts`, and `frontend/e2e/tests/settings/uploads-cleanup.spec.ts`. Verification caveats: repo-wide `go test ./...` still fails only in pre-existing tmp duplicate-main package; frontend typecheck still has unrelated pre-existing errors; Playwright global setup logs backend connection refused unless local backend is running.