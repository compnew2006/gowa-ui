Style/conventions: Go backend uses package-oriented design under `internal/handlers`, `internal/middleware`, `internal/tenant`, etc., with GORM for DB access and fasthttp for HTTP. Vue frontend uses Vue 3 + Vite + Pinia + TypeScript, with tests in Vitest/Playwright. Prefer targeted tests for touched areas, keep changes minimal in blast radius, and avoid reverting unrelated dirty worktree changes. Production work commonly updates `/summery.md` and deployment docs after verification. After code changes, run focused backend tests plus relevant frontend tests/builds; for deploys, verify the live services and critical endpoints/browser flows.